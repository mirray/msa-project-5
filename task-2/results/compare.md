| Критерий                                                                              | Spring Batch                                                                                                                                                                                     | Apache Airflow                                                                                                                                                     | K8s Job                                                                                                                                                         | Spark                                                                                                                                 |
|:--------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|:-------------------------------------------------------------------------------------------------------------------------------------------------------------------|:----------------------------------------------------------------------------------------------------------------------------------------------------------------|:--------------------------------------------------------------------------------------------------------------------------------------|
| **Наличие конфигурации CRON-расписания**                                              | Средняя (Внешний CronJob K8s или `@Scheduled(cron="0 0 6 * * ?")`. Требует внимания к timezone в поде)                                                                                           | **Высокая** (Нативный Cron с timezone в DAG: `schedule='0 6 * * *'`. Гарантированно сработает в 06:00 по часовому поясу магазина)                                  | **Высокая** (K8s CronJob: `schedule: "0 6 * * *"`. Нативно, надёжно, timezone через `timeZone` в манифесте)                                                     | **Избыточно** (Своего планировщика нет. Требуется Spark Operator CronSchedule или внешний Airflow)                                    |
| **Сложность реализации (SQL -> CSV/XLSX)**                                            | **Низкая** (Apache POI для XLSX из коробки. `JdbcCursorItemReader` + `ItemProcessor` (форматирование) + `FlatFileItemWriter`/`XlsxItemWriter`. Шрифты, колонтитулы, автоширина — 100 строк Java) | **Низкая** (Python + psycopg2 + openpyxl/xlsxwriter. Форматирование ячеек, автофильтры, закрепление шапки. 30 строк кода. Самое гибкое решение для форматирования) | **Средняя** (Python-скрипт в контейнере — легко. Но без фреймворка: самому управлять соединением, memory для XLSX-генерации, обработкой ошибок. ~80 строк кода) | **Избыточно** (Spark пишет CSV без форматирования. XLSX — не нативный формат, через плагины. Нет смысла)                              |
| **Ресурсоёмкость (10 000 строк XLSX)**                                                | **Низкая** (~200 МБ RAM. Генерация XLSX через POI займет 1-2 секунды + время SQL-запроса. Поднимается в рамках микросервиса)                                                                     | **Высокая** (Постоянный кластер 1+ ГБ RAM. Воркер Airflow выполнит задачу за 3 секунды, но сам Airflow "ест" ресурсы 24/7)                                         | **Крайне низкая** (Pod 50-100 МБ RAM. Запуск, выполнение за 3 секунды, удаление. Zero-cost между запусками)                                                     | **Высокая** (JVM 1+ ГБ RAM. Даже local mode — оверхед. Неоправданно)                                                                  |
| **Масштабируемость**                                                                  | **Не требуется** (Один под справляется за секунды. Распараллеливание бессмысленно)                                                                                                               | **Не требуется** (Один воркер Airflow справляется моментально)                                                                                                     | **Не требуется** (Один Pod — идеальное решение)                                                                                                                 | **Не требуется** (Spark-кластер для 10 000 строк — абсурд)                                                                            |
| **Сложность развёртывания в облаке и интеграция с микросервисами**                    | **Высокая** (Если Spring уже в стеке — идеально. Job встраивается в существующий микросервис `pricing-service` или `export-service`. Секреты БД и S3 уже есть. CI/CD через Jenkins/GitLab CI)    | **Низкая** (Managed Airflow (GCP Composer/AWS MWAA) — развернуть легко. Но интеграция с секретами микросервисов (Vault) и CI/CD DAG-ов сложнее)                    | **Высокая** (CronJob YAML в Git рядом с микросервисами. ArgoCD/Flux применят манифест. ServiceAccount для S3. Никакой новой инфраструктуры)                     | **Низкая** (Managed Spark — лишняя инфраструктура и затраты. Интеграция с CI/CD сложнее)                                              |
| **Удобство интеграции с логированием и мониторингом (гарантия доставки прайс-листа)** | **Высокая** (Spring Actuator + Micrometer. JobExecution статус в БД. Prometheus алерт: `spring_batch_job_failed > 0`. Легко отследить факт генерации файла и его загрузки в S3)                  | **Высокая** (Airflow UI: красный/зелёный DAG. SLA-алерты: если DAG не завершился к 06:10 — уведомление в Slack/Email. Лучший визуальный контроль)                  | **Высокая** (K8s Events + Loki + AlertManager. Алерт: `kube_job_failed{job="b2b-pricelist"} > 0`. Логи подов в Grafana. Просто и надёжно)                       | **Средняя** (Spark UI — лишняя сложность. History Server нужно настраивать. Для такой простой задачи мониторинг через Spark неудобен) |
| **Надёжность срабатывания ровно в 06:00**                                             | **Средняя** (Зависит от внешнего CronJob K8s. Если Job встроен в микросервис — нужен `@Scheduled`, но при рестарте пода время может сместиться. Лучше вынести в отдельный CronJob)               | **Высокая** (Airflow гарантирует запуск DAG по расписанию. Если в 06:00 кластер жив — DAG запустится. Пропущенные запуски выполняются при восстановлении)          | **Высокая** (K8s CronJob — battle-tested. Запускает Job ровно по Cron. При пропуске (кластер был недоступен) можно настроить `startingDeadlineSeconds`)         | **Средняя** (Зависит от внешнего триггера. Если Airflow упал — Spark Job не запустится. Двойная точка отказа)                         |

Вывод:K8s CronJob + Python script